# Optional: increases GitHub API rate limits for the server runtime.
# Never expose this value to the Studio view or commit a populated .env.local file.
GITHUB_TOKEN=

# Optional Phase 2 runtime activation from a published Studio version.
STUDIO_PROJECT_ID=
STUDIO_SUPABASE_URL=
STUDIO_SUPABASE_SERVICE_ROLE_KEY=
